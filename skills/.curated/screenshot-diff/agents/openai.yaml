interface:
  display_name: "Screenshot Diff"
  short_description: "Compare before/after images with rasterdiff."
  default_prompt: "Use $screenshot-diff to compare before.png and after.png and write the diff image to output.png."
